<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="me.horzwxy.app.pfm.android"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-permission
        android:name="android.permission.INTERNET"/>
    <uses-permission
        android:name="android.permission.GET_ACCOUNTS"/>

    <uses-sdk
        android:minSdkVersion="8"
        android:targetSdkVersion="17" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name="me.horzwxy.app.pfm.android.activity.LogInActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="me.horzwxy.app.pfm.android.activity.NewDiningActivity"
            android:label="@string/app_name"/>
        <activity
            android:name=".activity.ChooseParticipantsActivity"
            android:label="@string/app_name"/>
        <activity
                android:name="me.horzwxy.app.pfm.android.activity.ListContactsActivity"
                android:label="@string/app_name"/>
        <activity
                android:name="me.horzwxy.app.pfm.android.activity.ListDiningsActivity"
                android:label="@string/app_name"/>
        <activity
                android:name="me.horzwxy.app.pfm.android.activity.ListBillsActivity"
                android:label="@string/app_name"/>
        <activity
                android:name="me.horzwxy.app.pfm.android.activity.MainActivity"
                android:label="@string/app_name"/>
    </application>

</manifest>
